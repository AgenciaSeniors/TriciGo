requests>=2.32
rapidfuzz>=3.0
rtree>=1.2
unidecode>=1.3
# `>=0.13` era engañoso: esa versión NUNCA existió (PyPI va 0.4→0.9, 1.0, 1.0.1)
# y por orden PEP 440 el rango resolvía de hecho a 1.x. Se deja explícito, con
# techo en 2 para que un major con cambios de ruptura no entre solo.
# NO es el culpable del fallo del 17/08: se midió que la corrida exitosa
# (run 31364944202) y la fallida (32002252161) instalaron LA MISMA 1.0.1 — el
# 404 del catálogo STAC fue transitorio del lado de Overture. Ver el reintento
# con backoff en .github/workflows/sync-pois.yml.
overturemaps>=1.0.1,<2
duckdb>=1.0
pyarrow>=15.0
pandas>=2.0
huggingface_hub>=0.24
supabase>=2.5
# PR 5 of POI parity (2026-05-24): apply_osm_delta.py uses stdlib gzip +
# xml.etree to parse .osc.gz directly (no pyosmium required). No new deps.

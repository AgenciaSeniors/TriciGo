// RIDE_CONFIG now lives in @tricigo/utils so both the mobile client and the
// web app share a single source of truth. Re-exported here to keep existing
// `@/config/ride` imports valid.
export { RIDE_CONFIG } from '@tricigo/utils';

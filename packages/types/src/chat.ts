// ============================================================
// TriciGo — Chat Types
// ============================================================

export interface ChatMessage {
  id: string;
  ride_id: string;
  sender_id: string;
  body: string;
  created_at: string;
}
